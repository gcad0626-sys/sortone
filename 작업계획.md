# 리액트 프로젝트 변환 작업 계획서

본 계획서는 `org/` 폴더에 퍼블리싱된 기존 정적 웹 리소스(HTML/CSS/JS)를 React (Vite) + TypeScript + Styled Components 환경으로 변환하기 위한 세부 단계 및 구조 설계입니다.

## 1. 프로젝트 개요 및 환경 설정
- **작업 디렉토리**: `D:\황인영\project001`
- **원본 위치**: `D:\황인영\project001\publishing\org` (퍼블리싱 리소스)
- **프레임워크**: React (Vite 빌드 도구 사용)
- **언어**: TypeScript
- **라우팅**: `react-router-dom` 적용, 기본 경로 `/`
- **스타일링**: `styled-components`

### 패키지 설치 계획
```bash
# Vite 기반 React+TS 프로젝트 생성 (디렉토리가 비어있는 경우)
npm create vite@latest . -- --template react-ts

# 필수 라이브러리 설치
npm install react-router-dom styled-components
npm install -D @types/styled-components
```

## 2. 디렉토리 구조 설계 (src 내부)
```text
src/
 ┣ assets/         # 이미지(png, svg 등 폰트 리소스)
 ┣ components/     # 공통 컴포넌트 (Header, Footer, Sidebar 등)
 ┣ pages/          # 페이지 단위 컴포넌트
 ┣ styles/         # 전역 스타일 및 테마 (GlobalStyle, theme.ts)
 ┣ hooks/          # 커스텀 훅 (data.js를 대체할 로직)
 ┣ types/          # TypeScript 타입 정의 (Memo, User 등)
 ┣ App.tsx         # 전역 라우터 및 테마 프로바이더 설정
 ┗ main.tsx        # React 엔트리 포인트
```

## 3. 컴포넌트 분리 계획 (`src/components`)
퍼블리싱 코드를 분석하여 중복되는 요소들을 공통 컴포넌트로 분리합니다.

- **`Header.tsx`**: 상단 헤더 영역 (뒤로가기, 타이틀, 햄버거 메뉴, 아바타 등 렌더링)
- **`Footer.tsx` (TabBar)**: 하단 네비게이션 탭 (Memo, Ai, Search, Profile)
- **`Sidebar.tsx`**: 햄버거 메뉴 클릭 시 나타나는 드로어 메뉴
- **`Layout.tsx`**: 화면 전체 래퍼(`.app`), Header와 Footer를 감싸고 페이지 컨텐츠를 `<Outlet />`으로 렌더링
- **`Modal.tsx`**: 각 페이지에서 사용되는 팝업(확인창, 백업/복원 등) 공통 레이아웃
- **`MemoCard.tsx`**: 메모 목록에 표시되는 개별 메모 카드

## 4. 페이지 구성 및 라우터 매핑 (`src/pages`)
총 11개의 HTML 파일을 React 페이지 컴포넌트로 변환합니다.

| 원본 HTML | 페이지 컴포넌트명 | 라우터 경로 | 비고 (특징) |
| --- | --- | --- | --- |
| `splash.html` | `SplashPage.tsx` | `/` (기본 진입) | 초기 로딩 후 로그인이나 홈으로 리다이렉트 |
| `login.html` | `LoginPage.tsx` | `/login` | 로그인 폼 및 카카오 로그인 |
| `signup.html` | `SignupPage.tsx` | `/signup` | 회원가입 폼 |
| `index.html` | `HomePage.tsx` | `/home` 또는 `/memo` | 메모 목록 메인 화면 (Footer 포함) |
| `search.html` | `SearchPage.tsx` | `/search` | 메모 검색 화면 (Footer 포함) |
| `ai.html` | `AIPage.tsx` | `/ai` | AI 기능 화면 (Footer 포함) |
| `profile.html`| `ProfilePage.tsx` | `/profile` | 프로필/설정 화면 (팝업 다수, Footer 포함) |
| `add.html` | `AddMemoPage.tsx` | `/add` | 새 메모 작성 및 수정 |
| `detail.html` | `DetailPage.tsx` | `/detail/:id` | 메모 상세 보기 |
| `archive.html`| `ArchivePage.tsx` | `/archive` | 보관함 목록 |
| `trash.html` | `TrashPage.tsx` | `/trash` | 휴지통 목록 |

## 5. 단계별 작업 진행 계획

### Step 1: 프로젝트 초기화 및 스타일 기초 설정
- Vite React TS 프로젝트 생성
- `common.css`의 `:root` 변수들을 `styled-components`의 `theme.ts`로 변환
- `GlobalStyle.ts` 생성하여 폰트(Pretendard) 및 리셋 CSS, `.app` 컨테이너 전역 스타일 적용
- 이미지 파일들을 `src/assets` 혹은 `public`으로 이동

### Step 2: 공통 레이아웃 컴포넌트 개발
- `Header`, `Footer(TabBar)`, `Sidebar` 컴포넌트를 `styled-components`로 작성
- `react-router-dom`의 `<Outlet>`을 활용한 `Layout` 컴포넌트 구축

### Step 3: 데이터 로직 변환 (State Management)
- 기존 `data.js`에서 localStorage 기반으로 동작하던 로직(메모 목록, 설정, 사용자 정보 등)을 커스텀 훅(`useMemoData`, `useAuth` 등) 또는 Context API로 마이그레이션

### Step 4: 개별 페이지 구현 (스타일 및 로직 연동)
- 각 HTML의 구조를 JSX로 변환
- 페이지 종속적인 CSS (예: `profile.css`, `index.css`)를 Styled Components로 변환
- DOM 직접 제어 로직(`index.js`, `profile.js` 등)을 React의 `useState`, `useEffect` 기반으로 리팩토링

### Step 5: 라우팅 적용 및 최종 테스트
- `App.tsx`에 모든 경로 라우터 연결
- 페이지 간 이동 테스트 (SPA 라우팅 전환)
- CSS 깨짐 현상 및 모바일 뷰포트(`max-width: 480px`) 대응 확인
