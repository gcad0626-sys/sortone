# SortOne 스마트 메모 앱 퍼블리싱 작업계획

## 1. 프로젝트 개요
- **서비스명**: SortOne
- **컨셉**: 메모를 작성하면 AI가 자동으로 태그/카테고리를 추천해 분류하고, 언제든 쉽고 빠르게 찾아볼 수 있는 스마트 메모 앱
- **구현 방식**: 순수 HTML/CSS/JavaScript, 화면 전환 없이 SPA(싱글 페이지) 방식으로 3개 화면 전환 처리
- **디바이스**: 모바일 우선, `max-width: 480px` 기준 레이아웃, 480px 이상은 중앙 정렬 + 여백 처리

## 2. 디자인 톤 정의
| 구분 | 색상 코드 | 용도 |
|---|---|---|
| 배경(Primary) | `#63A8ED` | 전체 배경, 헤더, 주요 버튼 배경 |
| 포인트(Accent) | `#9CEAEF` | 강조 버튼(저장 버튼, FAB), 보조 포인트 |
| 화이트(Base) | `#FFFFFF` | 카드 배경, 텍스트(배경 위), 아이콘 배경 |

- **톤앤매너**: 미니멀, 카드 리스트 기반 UI, 둥근 모서리(radius 큼), 파스텔톤 태그 칩
- **폰트**: 로고는 손글씨 느낌의 라운드 서체(예: 스플래시의 "SortOne" 로고체), 본문은 시스템 기본 산세리프 폰트 사용 예정
- **공통 요소**: 상단 헤더(로고+검색+설정+프로필 아이콘), 하단 탭바(Memo / Ai / Search / Profile)

## 3. 화면별 구성 요소 분석

### 3-1. 스플래시 (splash.html)
- 배경색 `#63A8ED` 단색 풀스크린
- 화면 정중앙에 "SortOne" 로고 텍스트 노출
- 일정 시간(예: 1.5~2초) 후 JS로 자동으로 메인 화면(index)으로 전환 (fade 전환 고려)

### 3-2. 메인 홈 (index.html)
- **상단 헤더**: 햄버거 메뉴 / 로고 / 검색 아이콘 / 설정 아이콘 / 프로필 아바타(원형, 이니셜)
- **카테고리 필터 탭**: 전체 / 업무 / 개인 / 아이디어 (알약형 버튼, 선택 시 흰색 배경 강조)
- **중요 메모 섹션**: 📌 중요 메모 라벨 + 카드 1개(핑크톤 배경, 날짜/제목/본문 요약/카테고리 태그)
- **최신 메모 섹션**: "최신 메모" 라벨 + 정렬 옵션(최신순 ▼) + 카드 리스트
  - 카드 구성: 상대날짜(어제, 2일전 등), 제목, 본문 요약(2줄 말줄임), 카테고리 태그(색상 구분), 더보기(···) 버튼
- **메모 추가 버튼(FAB)**: 우측 하단 원형 + 아이콘, 포인트 컬러(#9CEAEF)
- **하단 탭바**: Memo(활성) / Ai / Search / Profile, 아이콘 + 라벨

### 3-3. 메모 작성 (add.html)
- 상단 헤더: 뒤로가기 / 로고 / 검색 / 설정 / 프로필
- 제목 입력 필드 (placeholder: "제목을 입력하세요..")
- 본문 입력 영역 (큰 텍스트박스, placeholder: "내용을 입력하세요..", 옅은 민트 배경)
- 텍스트박스 우측 하단 저장(체크) 버튼 (원형, 포인트 컬러)
- "AI 추천 태그" 라벨 + 추천 태그 칩 리스트(색상 다양, 클릭 시 선택/해제 토글 가능하도록 구현 예정)
- 하단 서식 툴바: Bold / Italic / 목록 / 이미지 첨부 / 링크 첨부 아이콘 (알약형 바 안에 배치)
- 저장 시 → 공통 데이터(memos 배열)에 새 메모 추가 → 메인 화면 "최신 메모" 리스트에 반영

### 3-4. AI 자동분류 상세 / Ai 탭 (ai.html)
- 상단 헤더: 햄버거 메뉴 / 로고 / 검색 / 설정 / 프로필 (메인 홈과 동일 헤더)
- **Weekly Insight 카드**: 화이트~연한 민트 그라데이션 카드
  - 라벨 "WEEKLY INSIGHT" + 타이틀 "이번 주의 핵심 통찰"
  - 본문 요약 문장(강조 색상으로 수치 하이라이트, 예: "15% 향상")
  - 하단에 2분할 통계 블록: "작성된 메모 24개" / "분석 효율 +12%"
- **분석 현황 섹션**: 섹션 타이틀 + 더보기(···) 버튼, 흰색 리스트 카드(항목명 + 진행률 or 상태 뱃지 우측 정렬)
- **실행 항목 섹션**: 섹션 타이틀, 흰색 리스트 카드(좌측 카테고리 뱃지(WORK/IDEA 컬러 구분) + 실행 항목 텍스트)
- **메모 추가 버튼(FAB)**: 메인 홈과 동일 위치/스타일
- **하단 탭바**: Ai 탭 활성화 상태

### 3-5. 스마트 검색 (search.html)
- 상단 헤더: 메인 홈과 동일
- **검색 입력 바**: 흰색 알약형 인풋 + 좌측 돋보기 아이콘 + placeholder "검색어를 입력하세요 (예: ㄴㅌ → 노트)" + 우측 원형 확인(체크) 버튼(포인트 컬러)
- **AI 스마트 태그 섹션**: "AI 스마트 태그" 라벨 + 추천 검색 태그 칩(파스텔톤, 클릭 시 해당 키워드로 검색)
- **최근 검색어 섹션**: 라벨 + 우측 "모두 지우기" 텍스트 버튼
  - 리스트 카드: 좌측 시계(최근) 아이콘 + 검색어 텍스트, 우측 개별 삭제(×) 버튼
- **메모 추가 버튼(FAB)**: 메인 홈과 동일
- **하단 탭바**: Search 탭 활성화 상태

### 3-6. 메모 상세보기 (detail.html)
- 상단 헤더: 뒤로가기 / 로고 / 프로필 (검색·설정 아이콘 없이 좌우 심플 구성)
- **AI 3줄 요약 카드**: 연한 옐로우그린 카드, ⭐ 아이콘 + "AI 3줄 요약" 라벨 + 불릿 3개(핵심 문장 요약)
- **본문 카드**: 흰색 배경, 문단 단위 원문 텍스트(스크롤 가능한 영역)
- **하단 탭바**: 메모 상세로 진입한 이전 탭(Memo 또는 Ai 등) 상태 유지, 기본은 Ai 탭 활성 예시
- 메인 홈 카드 클릭 또는 Ai 탭의 항목 클릭 시 해당 메모 데이터를 받아 상세 화면에 렌더링(제목은 헤더 없이 본문 상단 요약으로 대체되는 구조라 별도 title 노출 영역은 없음 — 필요 시 본문 카드 상단에 제목 추가 검토)

### 3-7. 프로필 (profile.html)
- 상단 헤더: 메인 홈과 동일(햄버거 / 로고 / 검색 / 설정 / 프로필 아바타)
- **프로필 정보 영역**: 중앙 정렬
  - 원형 프로필 사진 + 우측 하단 편집(연필) 버튼(다크 원형 배지)
  - 이름 "Alex Morgan"(볼드, 큰 사이즈) + 이메일 "alex@sortone.ai"
  - 뱃지 2개: "Pro Member", "Active since 2023" (알약형, 연한 톤)
- **지능형 관리 카드**: 흰색 카드
  - 섹션 타이틀 "✧ 지능형 관리"
  - "AI 자동 분류 모드" 라벨 + 설명("입력된 메모를 AI가 자동으로 카테고리화합니다.") + 우측 토글 스위치(온/포인트 컬러)
- **계정 및 데이터 카드**: 흰색 카드
  - 섹션 타이틀 "🗄 계정 및 데이터"
  - 리스트 항목 2개(연한 회색 배경 행): "데이터 백업 및 복구", "활동 기록 내보내기" — 좌측 아이콘 + 텍스트, 우측 화살표(>)
- **로그아웃 버튼**: 포인트 컬러(민트) 배경, 알약형, 아이콘 + 텍스트
- **계정 삭제 버튼**: 경고 톤(오렌지) 배경, 알약형, 아이콘 + 텍스트
- **하단 탭바**: Profile 탭 활성화 상태

### 3-8. 햄버거 메뉴 (사이드 드로어, 공통 오버레이 컴포넌트)
- 별도 페이지가 아니라, 각 화면 헤더의 햄버거(☰) 버튼 클릭 시 **왼쪽에서 슬라이드 인되는 드로어(오버레이)** 형태로 구현
- 배경(연한 회색) 위에 드로어 패널(폭 약 85%), 우측에 배경 화면이 살짝 보이는 딤(dim) 처리
- **상단 프로필 요약**: 원형 프로필 사진(작게) + 이름 "Alex Morgan" + "Pro Member" 뱃지 + 이메일
- **메모 카테고리 메뉴 리스트**: 아이콘 + 텍스트 (전체 메모 / 개인 / 업무 / 아이디어), 클릭 시 해당 카테고리로 필터링된 메인 화면으로 이동
- **구분선**
- **보관함 / 휴지통 메뉴**: 아이콘 + 텍스트
- **하단 고정 영역**: 구분선 + "설정" + "로그아웃"(빨간 텍스트) 메뉴
- 드로어 바깥(딤 영역) 클릭 또는 별도 닫기 동작으로 드로어를 닫을 수 있도록 구현
- 모든 화면(splash 제외)의 헤더 햄버거 버튼에서 공통으로 재사용

### 3-9. 로그인 (login.html)
- 배경 `#63A8ED` 단색 풀스크린(헤더/탭바 없음)
- 화면 상단부에 캐치프레이즈 문구: "생각을 명확한 인사이트로 바꾸세요." (중앙 정렬, 2줄)
- 화면 중하단부에 소셜 로그인 버튼 2개(세로 배치)
  - "카카오로 계속하기" — 노란색(#FEE500) 배경 + 말풍선 아이콘 + 검정 텍스트
  - "Google 계정으로 계속하기" — 흰색 배경 + Google 로고 아이콘 + 검정 텍스트
- 구분선 + "또는" 텍스트
- 하단: "이메일로 가입하기" 링크(볼드) + "이미 계정이 있으신가요? 로그인" 링크(밑줄)
- 로그인 성공 시 이동할 화면은 확인 필요(7번 확인사항 참고) — 우선 스플래시 이후 최초 진입 화면으로 배치하고, 소셜/이메일 버튼은 실제 인증 없이 UI 동작(클릭 시 `index.html`로 이동)만 구현 예정

### 3-10. 보관함 (archive.html)
- 상단 헤더: 메인 홈과 동일(햄버거 / 로고 / 검색 / 설정 / 프로필)
- **타이틀 영역**: 중앙 정렬
  - "보관함" (굵고 큰 타이틀)
  - 안내 문구: "보관된 메모는 검색에서만 제외되며 언제든지 복원할 수 있습니다." (2줄, 연한 톤)
- **보관 메모 리스트**: 흰색 카드
  - 좌측 상단 카테고리 뱃지(연한 회색/그레이톤 알약, 영문 라벨 예: `Product Strategy`, `Creative`, `Archives`) + 우측 날짜
  - 제목(볼드) + 본문 요약(2줄)
  - 카드에 썸네일 이미지가 있는 경우 우측에 정사각형 썸네일 노출(선택적)
  - 카드 우측 상단 복원 아이콘 버튼(상자에서 위로 화살표, 클릭 시 보관 해제 → 메인 리스트로 복귀)
- **하단 안내 문구**: "이전의 소중한 생각들이 안전하게 보관되어 있습니다." (리스트 하단, 연한 톤, 중앙 정렬)
- **하단 탭바**: 진입 경로(햄버거 메뉴)상 특정 탭에 종속되지 않는 화면 — 활성 탭 표시 방식은 확인 필요(12번 확인사항 참고)

### 3-11. 휴지통 (trash.html)
- 상단 헤더: 메인 홈과 동일
- **타이틀 영역**: 중앙 정렬
  - "휴지통" (굵고 큰 타이틀)
  - 개수 뱃지: "4 개의 메모" (알약형, 흰색 배경)
  - 안내 문구: "휴지통의 메모는 30일 후 영구 삭제됩니다."
- **삭제된 메모 리스트**: 흰색 카드
  - 좌측 상단 카테고리 뱃지(색상 구분: WORK/IDEA/PRIVATE/STUDY 등) + 우측 상대 삭제일("2일 전 삭제")
  - 제목(볼드) + 본문 요약(2줄, 썸네일 이미지가 있는 경우 카드 내 별도 영역에 노출)
  - 카드 하단 좌우 버튼: "↺ 복구"(좌측) / "🗑 영구 삭제"(우측, 빨간 텍스트)
- **하단 탭바**: 보관함과 동일하게 활성 탭 처리 방식 확인 필요

## 4. 파일 구조

```
/
├── splash.html
├── login.html            # 로그인 화면 [신규]
├── index.html
├── add.html
├── ai.html              # AI 자동분류 상세 (Ai 탭) 화면 [신규]
├── search.html          # 스마트 검색 화면 [신규]
├── detail.html          # 메모 상세보기 화면 [신규]
├── profile.html          # 프로필 화면 [신규]
├── archive.html          # 보관함 화면 [신규]
├── trash.html            # 휴지통 화면 [신규]
├── css/
│   ├── common.css      # CSS 변수(색상/폰트/radius 등), reset, 공통 레이아웃(헤더, 탭바, 버튼, 태그 칩 등)
│   ├── sidebar.css      # 햄버거 메뉴(사이드 드로어) 공통 오버레이 스타일 [신규]
│   ├── splash.css      # 스플래시 화면 전용 스타일
│   ├── login.css        # 로그인 화면 전용 스타일 [신규]
│   ├── index.css       # 메인 화면 전용 스타일
│   ├── add.css         # 메모 작성 화면 전용 스타일
│   ├── ai.css           # AI 탭 화면 전용 스타일 [신규]
│   ├── search.css       # 검색 화면 전용 스타일 [신규]
│   ├── detail.css       # 메모 상세보기 전용 스타일 [신규]
│   ├── profile.css      # 프로필 화면 전용 스타일 [신규]
│   ├── archive.css      # 보관함 화면 전용 스타일 [신규]
│   └── trash.css        # 휴지통 화면 전용 스타일 [신규]
└── js/
    ├── data.js          # 공통 데이터 관리 로직 (memos 배열, CRUD 함수, localStorage 연동)
    ├── common.js        # 공통 UI 로직 (헤더/탭바 렌더링, 화면 전환 유틸 등 필요 시)
    ├── sidebar.js        # 햄버거 메뉴(드로어) 열기/닫기, 카테고리 메뉴 클릭 시 필터 이동 로직 [신규]
    ├── splash.js         # 스플래시 → 메인 자동 전환 로직
    ├── login.js           # 소셜/이메일 로그인 버튼 클릭(UI 전용) 로직 [신규]
    ├── index.js          # 메인 화면 렌더링/필터링/카드 클릭 등 로직
    ├── add.js            # 메모 작성/태그 선택/저장 로직
    ├── ai.js              # Weekly Insight/분석 현황/실행 항목 렌더링 로직 [신규]
    ├── search.js          # 검색어 입력, 최근 검색어 관리(추가/삭제/전체삭제), AI 스마트 태그 클릭 로직 [신규]
    ├── detail.js          # 메모 상세 데이터 로딩 + AI 3줄 요약 렌더링 로직 [신규]
    ├── profile.js          # 프로필 정보 렌더링, AI 자동분류 모드 토글, 로그아웃/계정삭제 버튼 로직 [신규]
    ├── archive.js          # 보관 메모 리스트 렌더링 + 복원 로직 [신규]
    └── trash.js            # 삭제된 메모 리스트 렌더링 + 복구/영구삭제 로직 [신규]
```

- 사이드 드로어(햄버거 메뉴)는 별도 페이지가 아닌 공통 오버레이 컴포넌트이므로, 각 화면 html의 `</body>` 직전에 동일한 드로어 마크업을 포함하고 `sidebar.css` / `sidebar.js`를 공통으로 로드하는 방식으로 구현 예정 (splash.html, login.html 제외)

- 화면 전환은 실제 페이지 이동(`location.href`) 방식으로 우선 구현하되, 추후 SPA 전환(하나의 컨테이너에서 JS로 화면 show/hide) 방식으로 쉽게 리팩토링 가능하도록 화면별 렌더링 함수를 분리해서 작성
  - ※ 요구사항이 "페이지 이동 없이 JS로 처리하는 SPA 방식"이므로, 실제로는 3개의 html 파일을 유지하되 `index.html` 하나에 3개 화면(section)을 두고 JS로 `show/hide` 토글하는 구조로 구현할지, 혹은 각 파일을 분리하되 데이터만 공유(localStorage)하는 멀티페이지 구조로 할지 화면 작업 시작 전에 확인 필요 (아래 5번 확인사항 참고)

## 5. 데이터 구조 설계 (memos 배열)

추후 React 전환을 고려해 명확한 객체 스키마로 설계합니다.

```js
// js/data.js

/**
 * @typedef {Object} Memo
 * @property {string} id            - 고유 ID (예: timestamp 또는 uuid)
 * @property {string} title         - 메모 제목
 * @property {string} content       - 메모 본문
 * @property {string[]} tags        - 카테고리/AI추천 태그 배열 (예: ['업무', '우선순위'])
 * @property {string} category      - 상단 필터용 대표 카테고리 ('전체' | '업무' | '개인' | '아이디어')
 * @property {boolean} isImportant  - 중요 메모 여부 (핀 고정)
 * @property {string} createdAt     - ISO 날짜 문자열 (생성일)
 * @property {string} updatedAt     - ISO 날짜 문자열 (수정일)
 */

const memos = [
  {
    id: "1",
    title: "신규 프로젝트 마일스톤",
    content: "다음 분기 핵심 과제와 세부 일정을 정리함. 디자인 시스템 고도화 및 접근성 가이드라인 준수가 최우선...",
    tags: ["업무", "우선순위"],
    category: "업무",
    isImportant: true,
    createdAt: "2023-10-24T09:00:00.000Z",
    updatedAt: "2023-10-24T09:00:00.000Z"
  }
  // ...
];
```

- 데이터 관리 함수 예정 목록: `getAllMemos()`, `getMemoById(id)`, `addMemo(memo)`, `updateMemo(id, data)`, `deleteMemo(id)`, `getMemosByCategory(category)`, `getImportantMemos()`
- 새로고침 시에도 데이터 유지가 필요하면 `localStorage` 연동 (화면 작업 시작 전 확인)

### 5-1. Memo 스키마 확장 — AI 3줄 요약 필드 [신규]
메모 상세보기(detail.html)의 "AI 3줄 요약" 카드를 위해 `Memo` 타입에 요약 필드를 추가합니다.

```js
/**
 * @property {string[]} aiSummary   - AI가 생성한 3줄 요약 (문장 배열, 없으면 빈 배열)
 */
```
- 실제 AI 연동 전이므로, 저장 시 본문 앞부분을 문장 단위로 잘라 더미로 채우거나 빈 배열로 두고 상세 화면에서 "요약 준비 중" 형태로 처리하는 방식을 검토합니다 (7번 확인사항 참고).

### 5-2. 검색 데이터 스키마 [신규]
검색어 입력/최근 검색어 관리를 위한 별도 데이터를 `js/data.js`(또는 `js/search.js` 내 `SearchStore`)에 정의합니다.

```js
// localStorage key: 'sortone_recent_searches'
// 최신순 문자열 배열로 관리 (예: ["2024년 사업 기획서 초안", "건축 미니멀리즘", "마케팅 회의록 03.12"])

const SearchStore = {
  getRecentSearches() {},      // 최근 검색어 목록 조회
  addRecentSearch(keyword) {}, // 검색 실행 시 최상단에 추가(중복 제거)
  removeRecentSearch(keyword) {}, // 개별 삭제(×)
  clearRecentSearches() {}     // "모두 지우기"
};
```
- **AI 스마트 태그**(프로젝트 기획 / 워크플로우 / 아이디어 등)는 현재 저장된 `memos`의 `tags`를 빈도순으로 집계하거나, add.html과 동일한 더미 키워드 풀에서 상위 몇 개를 뽑아 노출하는 방식으로 구현 예정입니다.

### 5-3. AI 인사이트(Weekly Insight) 더미 데이터 [신규]
`ai.html`의 통계/리스트는 실제 분석 엔진이 없으므로, `memos` 배열에서 계산 가능한 값(예: "작성된 메모 24개" → `memos.length`)은 실데이터로 연결하고, "분석 효율 +12%" 같은 지표와 분석 현황/실행 항목 리스트는 더미 상수 데이터로 별도 정의합니다.

```js
// js/ai.js 내 더미 데이터 예시
const weeklyInsight = {
  summaryText: "프로젝트 관리와 효율적 소통이 업무 생산성을 15% 향상시켰습니다.",
  highlightValue: "15%",
  memoCount: null,        // MemoStore.getAllMemos().length 로 실시간 계산
  analysisEfficiency: "+12%"
};

const analysisStatus = [
  { label: "시장 분석 보고서 요약", value: "85%" },
  { label: "미팅 로그 #204 클러스터링", value: "완료" }
];

const actionItems = [
  { category: "WORK", text: "디자인 팀 검토 미팅 예약" },
  { category: "IDEA", text: "사용자 인터뷰 질문지 리스트업" }
];
```

### 5-4. 사용자 프로필 / 설정 데이터 스키마 [신규]
프로필 화면(profile.html)과 햄버거 메뉴(sidebar) 상단에서 공통으로 쓰는 사용자 정보와, AI 자동 분류 모드 on/off 상태를 관리합니다.

```js
// localStorage key: 'sortone_user'
const user = {
  name: "Alex Morgan",
  email: "alex@sortone.ai",
  avatarUrl: "",              // 이미지 미첨부 시 이니셜(IY 등) 뱃지로 대체
  membership: "Pro Member",
  activeSince: "2023"
};

// localStorage key: 'sortone_settings'
const settings = {
  aiAutoClassify: true        // 프로필 화면 토글 스위치 상태
};

const ProfileStore = {
  getUser() {},
  getSettings() {},
  toggleAiAutoClassify() {},  // 토글 클릭 시 localStorage 값 반전
  logout() {},                // 더미 로그아웃(로그인 상태 초기화 후 login.html로 이동)
  deleteAccount() {}          // 더미 계정 삭제(확인창 후 전체 데이터 초기화)
};
```
- 로그인 여부는 `localStorage`의 `sortone_logged_in`(boolean) 값으로 간단히 관리하고, 실제 OAuth 연동은 하지 않습니다(7번 확인사항 참고).

### 5-5. Memo 스키마 확장 — 보관/휴지통 필드 [신규]
보관함(archive.html)·휴지통(trash.html)을 위해 `Memo` 타입에 상태 필드를 추가합니다. 삭제는 즉시 제거가 아닌 **소프트 삭제**(`isDeleted` 플래그)로 처리해 30일 후 영구 삭제 안내와 "복구" 기능을 지원합니다.

```js
/**
 * @property {boolean} isArchived   - 보관함 이동 여부 (true면 메인 리스트/검색에서 제외)
 * @property {boolean} isDeleted    - 휴지통 이동 여부 (소프트 삭제)
 * @property {string|null} deletedAt - 삭제(휴지통 이동) 시각 ISO 문자열, 없으면 null
 * @property {string} [thumbnailUrl] - 카드에 썸네일 이미지가 있는 경우(선택)
 */
```

`MemoStore`에 아래 함수를 추가합니다.
```js
getArchivedMemos()          // isArchived === true (isDeleted === false) 목록
archiveMemo(id)              // isArchived = true 로 전환
unarchiveMemo(id)            // 보관함 카드의 "복원" 버튼 → isArchived = false

getTrashedMemos()            // isDeleted === true 목록
softDeleteMemo(id)           // isDeleted = true, deletedAt = 현재시각 (휴지통 이동)
restoreMemo(id)              // 휴지통 카드의 "복구" 버튼 → isDeleted = false, deletedAt = null
permanentlyDeleteMemo(id)    // 배열에서 완전히 제거 (휴지통 카드의 "영구 삭제" 버튼)
```
- `getAllMemos()` / `getMemosByCategory()` 등 메인 홈에서 쓰는 조회 함수들은 `isArchived === false && isDeleted === false`인 메모만 반환하도록 필터 조건을 함께 보강합니다(보관/삭제된 메모가 메인 리스트나 검색 결과에 다시 노출되지 않도록).

## 6. 작업 순서 (요청 주신 순서 기준)

1. **공통 스타일 정의** — `css/common.css`
   - CSS 변수 정의(`--color-primary`, `--color-accent`, `--color-white` 등)
   - 리셋 CSS, 기본 폰트/타이포그래피
   - 공통 컴포넌트 스타일(헤더, 하단 탭바, 카드, 태그 칩, 버튼, FAB) 뼈대 정의
   - `js/data.js` 데이터 구조 및 초기 더미 데이터 작성

2. **메인 화면(index.html) 작업**
   - 헤더 + 카테고리 필터 + 중요 메모 + 최신 메모 리스트 + FAB + 하단 탭바 마크업/스타일
   - `js/index.js`: 데이터 렌더링, 카테고리 필터링, 카드 리스트 출력

3. **스플래시 화면(splash.html) 작업**
   - 로고 노출 마크업/스타일
   - `js/splash.js`: 일정 시간 후 메인 화면 자동 전환 로직

4. **메모 작성 화면(add.html) 작업**
   - 제목/본문 입력, AI 추천 태그, 서식 툴바, 저장 버튼 마크업/스타일
   - `js/add.js`: 입력값 수집, 태그 선택 토글, 저장 시 `data.js`를 통해 memos 배열에 추가 후 메인 화면 리스트 반영

5. **AI 자동분류 상세 화면(ai.html) 작업** [신규]
   - Weekly Insight 카드 + 분석 현황 리스트 + 실행 항목 리스트 + FAB + 하단 탭바(Ai 활성) 마크업/스타일
   - `js/ai.js`: 더미 인사이트 데이터 렌더링, 작성된 메모 수는 `MemoStore.getAllMemos().length`로 실시간 계산해 반영, 리스트 항목 클릭 시 `detail.html`로 이동

6. **스마트 검색 화면(search.html) 작업** [신규]
   - 검색 입력 바 + AI 스마트 태그 + 최근 검색어 리스트 + FAB + 하단 탭바(Search 활성) 마크업/스타일
   - `js/search.js`: 검색 실행/최근 검색어 추가·개별삭제·전체삭제, 스마트 태그 클릭 시 검색어 자동 입력 및 검색 실행

7. **메모 상세보기 화면(detail.html) 작업** [신규]
   - AI 3줄 요약 카드 + 본문 카드 + 뒤로가기 헤더 + 하단 탭바 마크업/스타일
   - `js/detail.js`: URL 파라미터(`?id=`)로 메모 데이터 로딩, `aiSummary` 필드 렌더링(없을 경우 대체 문구 처리), 메인 홈/Ai 탭 리스트 항목 클릭 시 진입 연결

8. **로그인 화면(login.html) 작업** [신규]
   - 캐치프레이즈 + 소셜 로그인 버튼 2종 + 구분선 + 이메일 가입/로그인 링크 마크업/스타일
   - `js/login.js`: 버튼 클릭 시 `sortone_logged_in` 값을 저장하고 `index.html`로 이동(실제 OAuth 연동 없음)

9. **프로필 화면(profile.html) 작업** [신규]
   - 프로필 정보(사진/이름/이메일/뱃지) + 지능형 관리 카드(토글) + 계정 및 데이터 카드 + 로그아웃/계정삭제 버튼 + 하단 탭바(Profile 활성) 마크업/스타일
   - `js/profile.js`: `ProfileStore`로 사용자 정보/AI 자동분류 토글 상태 렌더링 및 저장, 로그아웃/계정삭제 버튼 동작 연결

10. **햄버거 메뉴(사이드 드로어) 공통 컴포넌트 작업** [신규]
    - 프로필 요약 + 카테고리 메뉴 리스트 + 보관함/휴지통 + 설정/로그아웃 마크업/스타일(`css/sidebar.css`)
    - `js/sidebar.js`: 드로어 열기/닫기(딤 클릭 시 닫힘), 카테고리 메뉴 클릭 시 `index.html?category=...`로 이동해 필터 적용
    - splash.html, login.html을 제외한 모든 화면(index/add/ai/search/detail/profile)의 헤더 햄버거 버튼에 연결

11. **보관함 화면(archive.html) 작업** [신규]
    - 타이틀 + 안내 문구 + 보관 메모 카드 리스트(복원 버튼 포함) + 하단 안내 문구 + 하단 탭바 마크업/스타일
    - `js/archive.js`: `MemoStore.getArchivedMemos()`로 목록 렌더링, 복원 버튼 클릭 시 `unarchiveMemo(id)` 호출 후 리스트 갱신

12. **휴지통 화면(trash.html) 작업** [신규]
    - 타이틀 + 개수 뱃지 + 안내 문구 + 삭제 메모 카드 리스트(복구/영구삭제 버튼 포함) + 하단 탭바 마크업/스타일
    - `js/trash.js`: `MemoStore.getTrashedMemos()`로 목록 렌더링, "복구" 클릭 시 `restoreMemo(id)`, "영구 삭제" 클릭 시 확인창 후 `permanentlyDeleteMemo(id)` 호출 후 리스트 갱신, 상대 삭제일("2일 전 삭제")은 `deletedAt` 기준으로 계산

## 7. 작업 시작 전 확인이 필요한 사항
1. **SPA 구현 방식**: (a) `index.html` 한 파일 안에 splash/main/add 3개 section을 두고 JS로 화면 토글하는 진짜 SPA로 만들지, (b) 3개 html 파일을 유지하되 데이터만 `localStorage`로 공유하는 멀티페이지 방식으로 할지
2. **데이터 영속성**: 새로고침/재접속 시에도 작성한 메모가 유지되어야 하는지 (localStorage 사용 여부)
3. **스플래시 자동 전환 시간**: 예상 노출 시간(예: 1.5초, 2초 등) 지정 여부
4. **AI 추천 태그**: 실제 AI 연동은 없으므로 더미 로직(랜덤 or 키워드 기반 간단 매칭)으로 대체해도 되는지
5. **AI 3줄 요약(detail.html) 생성 방식** [신규]: 메모 저장 시 본문을 문장 단위로 잘라 더미로 3줄을 채울지, 미리 정의된 샘플 요약만 특정 메모(시연용)에 넣어둘지 확인 필요
6. **Weekly Insight 지표(ai.html) 성격** [신규]: "15% 향상", "분석 효율 +12%" 등은 고정 더미 값으로 두고 "작성된 메모 24개"만 실제 개수로 연동하는 방향으로 진행해도 되는지
7. **메모 상세보기 진입 경로** [신규]: 메인 홈 카드 클릭 시에도 `add.html`(수정 모드) 대신 `detail.html`(상세보기)로 먼저 이동하고, 상세 화면에서 "수정" 버튼을 눌러야 `add.html`로 넘어가는 흐름으로 변경할지, 아니면 기존처럼 카드 클릭 = 바로 수정 화면으로 유지할지
8. **로그인/인증 처리 범위** [신규]: 카카오/Google/이메일 로그인은 실제 OAuth 연동 없이 클릭 시 바로 `index.html`로 이동하는 더미 UI로만 구현해도 되는지, 앱 최초 진입 흐름을 `splash → login → index`로 확정해도 되는지
9. **비로그인 접근 제어** [신규]: `login.html`을 거치지 않고 `index.html` 등 URL을 직접 입력해 들어왔을 때도 그대로 접근을 허용할지(현재 계획은 별도 인증 가드 없이 전 화면 접근 가능하게 둘 예정), 접근 제한이 필요하면 별도 가드 로직 추가 필요
10. **프로필의 계정 삭제/로그아웃 동작 범위** [신규]: "계정 삭제" 클릭 시 실제로 `localStorage`의 모든 메모/검색어/설정 데이터를 초기화할지, 단순 확인 alert만 띄우고 실제 삭제는 하지 않을지
11. **햄버거 메뉴 카테고리 메뉴 동작** [신규]: "개인/업무/아이디어" 클릭 시 `index.html`로 이동 후 해당 카테고리 필터가 자동 적용되는 것으로 구현해도 되는지, "보관함"/"휴지통"은 이번 단계에서는 UI만 배치하고 실제 기능(보관/삭제 이동)은 추후 별도 화면에서 구현할지
12. **보관함/휴지통의 하단 탭바 활성 상태** [신규]: 두 화면 모두 특정 탭(Memo/Ai/Search/Profile)에 종속되지 않는데, 시안(이미지1)에는 "Ai" 탭이 활성 상태로 표시되어 있습니다 — 이를 고정값으로 유지할지, 혹은 두 화면 모두 하단 탭바에서 어떤 탭도 활성 표시하지 않을지 확인 필요
13. **영구 삭제 데이터 처리** [신규]: 휴지통의 "영구 삭제" 버튼 클릭 시 확인창을 거쳐 실제로 `localStorage`에서 해당 메모를 완전히 제거할지(현재 계획), 아니면 UI 동작(alert)만 보여주고 데이터는 남겨둘지
14. **메인 홈/검색 결과에서 보관·삭제 메모 제외 범위** [신규]: 계획상 보관/휴지통으로 이동한 메모는 메인 홈의 "최신 메모"·"중요 메모" 리스트와 검색 결과에서 자동으로 제외되도록 `MemoStore` 조회 함수를 보강할 예정인데, 이 방향으로 진행해도 되는지(현재 이미 만들어진 index.js/search.js 등의 조회 로직도 함께 수정하게 됩니다)

---
위 계획 확인해 주시면, 이어서 요청 주시는 순서(11. 보관함 → 12. 휴지통)대로 화면별로 코드 작업을 진행하겠습니다.
